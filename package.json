{
  "name": "codex-script-loader",
  "version": "0.0.1",
  "private": true,
  "description": "A local, CDP-based script loader for Codex Desktop.",
  "type": "module",
  "bin": {
    "codex-script-loader": "src/cli.mjs"
  },
  "scripts": {
    "test": "node --test",
    "check": "node --check src/cli.mjs && node --check src/manager-server.mjs && node --check prototype/api-client.js && node --check prototype/app.js && node --check scripts/browser-smoke.mjs",
    "smoke:ui": "node scripts/browser-smoke.mjs",
    "serve": "node src/cli.mjs serve"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
