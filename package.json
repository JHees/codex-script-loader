{
  "name": "codex-script-loader",
  "version": "0.0.1",
  "private": true,
  "description": "A local, CDP-based script loader for Codex Desktop.",
  "type": "module",
  "bin": {
    "codex-script-loader": "src/cli.mjs"
  },
  "scripts": {
    "test": "node --test",
    "check": "node --check src/cli.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
